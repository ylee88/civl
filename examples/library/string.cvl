#include<string.h>
#include<stdlib.h>

void main(){
  char* s;
  int len;
  
  s = (char*)malloc(sizeof(char) * 4);
  
  //s[0] = 'a';
  //s[1] = 'b';
  strcpy(s, "abcd");
  char * q = "srgre";
  int x = strcmp(q, "abcd");
  len = strlen(s);
  len = strlen(s + 1);
  free(s);
}
