#include <civlc.h>

enum hue { chartreuse, burgundy, claret=20, winedark };

void main(){
  enum hue col, *cp;
  
  col = burgundy;
  cp = &col;
  if (*cp != claret)
    *cp = claret;
  $assert(col == 20);
}
