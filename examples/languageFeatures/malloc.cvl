/* Commandline execution:
 *		civl verify malloc.cvl
 * */
#include <civlc.h>

void main() {
  $scope s;
  double *p = (double *) $malloc<s>(5*sizeof(double));
  
  p[4]=3.14;
  $assert(p[4]==3.14);
  $free(p);
}   
