/* Example to demonstrate the use of system function equalsTo() that 
 * compares two process references.
 * 
 */
#include <civlc.h>
#include <stdio.h>

void main() {
  $proc p = $self, q = $self;
  _Bool result = equalsTo(p,q);
  
  $assert(result);
}
