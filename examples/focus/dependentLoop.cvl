#include<assert.h>

#pragma CIVL ACSL
$input int N;
$assume(0 < N);
int a[N];

int main() {

  a[0] = 0;
  //@ focus I;
  /*@ loop invariant a[i-1] == 0;
    @*/
  for (int i = 1; i < N; i++) {
    a[i] = a[i-1];
  }

  //@ focus I;
  $assert($forall(int i:0..N-1) a[i] == 0);
}
