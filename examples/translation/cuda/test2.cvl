
#include "civlc.h" 
#include <stdio.h>

_Bool isPowerOfTwo(int x) {
	if (x == 1) {
		return $true;
	} else {
		return x % 2 == 0 && isPowerOfTwo(x / 2);
	}
}

$input int N;
$input int B;
$assume(0 <= N && N <= B);
$assume(isPowerOfTwo(N));

int main (int argc, char** argv) {
	printf("%d\n", N);
}
