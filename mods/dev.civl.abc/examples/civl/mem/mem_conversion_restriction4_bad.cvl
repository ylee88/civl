int main() {
  int *a[2];
  int b[2];

  a[0] = &b[0];
  a[1] = &b[1];
  // pointer set can only be constructed recursively using two ways:
  // 1. &LHS
  // 2. pointer (set) + integer/range
  $mem m = a[0 .. 1] + 1; 
  return 0;
}
