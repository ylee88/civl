/**
 * The <strong>antlr2ast</strong> module provides a translator which
 * takes an ANTLR tree representation of a program
 * and produces an ABC Abstract Syntax Tree.
 * 
 * @see dev.civl.abc.front.IF.ASTGenerator
 */
/**
 * <p>Module <strong>parse</strong> is used to parse a token stream and
 * produce an ANTLR tree representation of a C program.  Typically, the token
 * stream emanates from a preprocessor, but it doesn't have to.</p>  
 */
package dev.civl.abc.front.IF;
