/**
 * The object package provides the
 * {@link dev.civl.sarl.IF.object.SymbolicObject SymbolicObject}
 * interface, which is the root of the symbolic object type hierarchy. In
 * addition, it provides interfaces for the "concrete" kinds of symbolic
 * objects: booleans, (small) Java <code>int</code>s,
 * {@link dev.civl.sarl.IF.number.Number Number}s, and
 * {@link java.lang.String String}s.
 */
package dev.civl.sarl.IF.object;